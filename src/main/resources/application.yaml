spring:
  datasource:
    url: jdbc:mysql://db-spotify.cd6umyukudf3.sa-east-1.rds.amazonaws.com:3306/spotifydb?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true
    username: admin
    password: "#Ruan1235"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect